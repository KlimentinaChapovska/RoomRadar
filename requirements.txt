# Root-level dev and tooling dependencies.
# Runtime dependencies for the API live in backend/requirements.txt.

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Linting / formatting (optional, run locally)
ruff>=0.4.0

# Data / ML (used in ml/ notebooks and scripts)
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
jupyter>=1.0.0
